apiVersion: v1
kind: Service
metadata:
  name: mariadb-product
  labels: { app: mariadb-product }
spec:
  clusterIP: None
  selector: { app: mariadb-product }
  ports:
    - name: mysql
      port: 3306
      targetPort: 3306
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mariadb-product
spec:
  serviceName: mariadb-product
  replicas: 1
  selector:
    matchLabels: { app: mariadb-product }
  template:
    metadata:
      labels: { app: mariadb-product }
    spec:
      containers:
        - name: mariadb
          image: mariadb:11.0
          ports:
            - containerPort: 3306
          envFrom:
            - secretRef: { name: mariadb-product-secret }   # MYSQL_* 사용
          volumeMounts:
            - name: data
              mountPath: /var/lib/mysql
          readinessProbe:
            tcpSocket: { port: 3306 }
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            tcpSocket: { port: 3306 }
            initialDelaySeconds: 15
            periodSeconds: 10
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: [ "ReadWriteOnce" ]
        resources:
          requests:
            storage: 10Gi
